## Summary

Describe what changed and why.

## Validation

- [ ] `npm run lint`
- [ ] `npm run build`
- [ ] `cd src-tauri && cargo check`

## Scope

- [ ] UI strings are localized (no new hardcoded user-facing text)
- [ ] No breaking config/data changes, or migration notes added
- [ ] Changes target `dev` branch (except emergency hotfix)
